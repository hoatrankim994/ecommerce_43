<% provide :title, @user.name %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
<h2><%= t "controller.products.purchase_history" %></h2>
<%= render partial: "history_orders", collection: @products, as: :item %>
